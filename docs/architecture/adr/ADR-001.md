# ADR-001: Pilot Canonical Architecture

Decision: LEVEX Pilot v1 uses a TypeScript modular monolith with PostgreSQL, application authorization plus RLS, an asynchronous worker, private object storage, transactional outbox, and structured audit. Critical business state is server-authoritative.

Status: accepted
